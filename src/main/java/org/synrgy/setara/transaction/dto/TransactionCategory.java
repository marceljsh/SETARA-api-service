package org.synrgy.setara.transaction.dto;

public enum TransactionCategory {
    ALL_TRANSACTIONS, // All transactions
    OUTGOING,         // Outgoing transactions (TRANSFER, TOP_UP)
    INCOMING         // Incoming transactions (DEPOSIT)

}
